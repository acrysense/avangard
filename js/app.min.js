"use strict";

function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }
function _iterableToArray(iter) { if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter); }
function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) return _arrayLikeToArray(arr); }
function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i]; return arr2; }
document.addEventListener('DOMContentLoaded', function () {
  // checker
  var useItemChecker = function useItemChecker(els, onClickOutside) {
    var checkBodyClick = function checkBodyClick(e) {
      var currentEl = e.target;
      while (currentEl) {
        if (els.includes(currentEl)) break;
        currentEl = currentEl.parentNode;
      }
      if (!currentEl) {
        onClickOutside();
        removeBodyChecker();
      }
    };
    function setBodyChecker() {
      document.documentElement.addEventListener('click', checkBodyClick);
    }
    function removeBodyChecker() {
      document.documentElement.removeEventListener('click', checkBodyClick);
    }
    return {
      setBodyChecker: setBodyChecker,
      removeBodyChecker: removeBodyChecker
    };
  };

  // inputmask
  Inputmask().mask(document.querySelectorAll('input'));

  // textarea
  var allTexareas = document.querySelectorAll('textarea');
  if (allTexareas) {
    allTexareas.forEach(function (el) {
      el.style.height = el.setAttribute('style', 'height: ' + el.scrollHeight + 'px');
      el.classList.add('auto');
      el.addEventListener('input', function (e) {
        el.style.height = 'auto';
        el.style.height = el.scrollHeight + 'px';
      });
    });
  }

  // change theme by clicking a button
  var toggleDarkThemeBtn = document.querySelectorAll('.theme-switch__input');
  var enableDarkMode = function enableDarkMode() {
    localStorage.theme = 'dark';
    document.documentElement.classList.add('dark');
  };
  var disableDarkMode = function disableDarkMode() {
    localStorage.theme = 'light';
    document.documentElement.classList.remove('dark');
  };
  if (toggleDarkThemeBtn) {
    toggleDarkThemeBtn.forEach(function (item) {
      item.addEventListener('click', function (event) {
        event.preventDefault();

        // if (localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        //     disableDarkMode();
        // } else {
        //     enableDarkMode();
        // }
      });
    });
  }

  // mobile menu
  var mobileMenu = document.getElementById('menu');
  var mobileMenuTrigger = document.getElementById('hamburger-toggle');
  var header = document.getElementById('header');
  if (header && mobileMenu && mobileMenuTrigger) {
    mobileMenuTrigger.addEventListener('click', function (event) {
      event.preventDefault();
      if (mobileMenu.classList.contains('is--active')) {
        header.classList.remove('is--menu');
        mobileMenuTrigger.classList.remove('is--active');
        mobileMenu.classList.remove('is--active');
        document.body.classList.remove('scroll-disabled');
      } else {
        header.classList.add('is--menu');
        mobileMenuTrigger.classList.add('is--active');
        mobileMenu.classList.add('is--active');
        document.body.classList.add('scroll-disabled');
      }
    });
  }

  // counter
  var counters = document.querySelectorAll('.about__value');
  var speed = 250;
  if (counters) {
    var formatNumber = function formatNumber(number) {
      return number.toLocaleString('ru-RU').replace(/,/g, ' ') + '+';
    };
    var animateCounter = function animateCounter(counter) {
      var target = +counter.getAttribute('data-target');
      var increment = target / speed;
      var updateCounter = function updateCounter() {
        var current = +counter.innerText.replace(/\s/g, '').replace('+', '');
        if (current < target) {
          counter.innerText = formatNumber(Math.ceil(current + increment));
          requestAnimationFrame(updateCounter);
        } else {
          counter.innerText = formatNumber(target);
        }
      };
      updateCounter();
    };
    var options = {
      threshold: 0.5
    };
    var observer = new IntersectionObserver(function (entries, observer) {
      entries.forEach(function (entry) {
        if (entry.isIntersecting) {
          animateCounter(entry.target);
          observer.unobserve(entry.target);
        }
      });
    }, options);
    counters.forEach(function (counter) {
      observer.observe(counter);
    });
  }

  // tabs
  var tabsItems = document.querySelectorAll('.tabs__item');
  if (tabsItems) {
    tabsItems.forEach(function (item, i) {
      item.addEventListener('click', function () {
        var parent = item.closest('.tabs');
        parent.querySelectorAll('.tabs__item').forEach(function (child) {
          return child.classList.remove('is--active');
        });
        parent.querySelectorAll('.tabs__holder').forEach(function (child) {
          return child.classList.remove('is--active');
        });
        item.classList.add('is--active');
        var el = parent.querySelector('.is--active');
        var index = _toConsumableArray(parent.querySelectorAll('.tabs__item')).indexOf(el);
        parent.querySelectorAll('.tabs__holder')[index].classList.add('is--active');
      });
    });
  }

  // select
  var selectBox = document.querySelectorAll('.select-box__placeholder');
  var selectBoxOption = document.querySelectorAll('.select-box__checkbox');
  if (selectBox) {
    selectBox.forEach(function (item) {
      var close = function close() {
        document.querySelectorAll('.select-box').forEach(function (child) {
          return child.classList.remove('is--active');
        });
      };
      var itemChecker = useItemChecker([item.parentNode].concat(_toConsumableArray(document.querySelectorAll('.select-box'))), close);
      item.addEventListener('click', function () {
        var parent = item.closest('.select-box');
        if (parent.classList.contains('is--active')) {
          close();
        } else {
          close();
          parent.classList.add('is--active');
          itemChecker.setBodyChecker();
        }
      });
    });
    if (selectBoxOption) {
      selectBoxOption.forEach(function (item, index) {
        item.addEventListener('click', function (event) {
          var parent = item.closest('.select-box');
          var placeholder = parent.querySelector('.select-box__placeholder span');
          placeholder.innerHTML = item.querySelector('.select-box__title').innerHTML;
          document.querySelectorAll('.select-box').forEach(function (child) {
            return child.classList.remove('is--active');
          });
          if (!parent.querySelector('.select-box__placeholder').classList.contains('is--selected')) {
            parent.querySelector('.select-box__placeholder').classList.add('is--selected');
          }
          if (parent.closest('.tabs__select')) {
            var _parent = item.closest('.tabs');
            _parent.querySelectorAll('.tabs__item').forEach(function (child) {
              return child.classList.remove('is--active');
            });
            _parent.querySelectorAll('.tabs__holder').forEach(function (child) {
              return child.classList.remove('is--active');
            });
            _parent.querySelectorAll('.tabs__item')[index].classList.add('is--active');
            _parent.querySelectorAll('.tabs__holder')[index].classList.add('is--active');
          }
        });
      });
    }
  }

  // main slider
  var mainSlider = document.querySelector('.main__slider .swiper');
  if (mainSlider) {
    var myMainSlider = new Swiper(mainSlider, {
      slidesPerView: 1,
      loop: true,
      pagination: {
        el: '.main__slider .swiper-pagination',
        type: 'bullets',
        clickable: true
      }
    });
  }

  // projects slider
  var projectsSlider = document.querySelector('.projects__slider .swiper');
  if (projectsSlider) {
    var myProjectsSlider = new Swiper(projectsSlider, {
      slidesPerView: 'auto',
      spaceBetween: 30,
      loop: true,
      navigation: {
        prevEl: '.projects__slider .swiper-button-prev',
        nextEl: '.projects__slider .swiper-button-next'
      },
      breakpoints: {
        0: {
          slidesPerView: 1,
          spaceBetween: 20
        },
        1200: {
          slidesPerView: 'auto',
          spaceBetween: 30
        }
      }
    });
  }

  // articles slider
  var articlesSlider = document.querySelector('.articles__slider .swiper');
  if (articlesSlider) {
    var myArticlesSlider = new Swiper(articlesSlider, {
      slidesPerView: 'auto',
      spaceBetween: 30,
      navigation: {
        prevEl: '.articles__slider .swiper-button-prev',
        nextEl: '.articles__slider .swiper-button-next'
      },
      breakpoints: {
        0: {
          spaceBetween: 20
        },
        1200: {
          spaceBetween: 30
        }
      }
    });
  }

  // documents slider
  var documentsSlider = document.querySelectorAll('.documents__slider .swiper');
  if (documentsSlider) {
    documentsSlider.forEach(function (slider) {
      var myDocumentsSlider = new Swiper(slider, {
        slidesPerView: 'auto',
        spaceBetween: 30,
        breakpoints: {
          0: {
            spaceBetween: 20
          },
          1200: {
            spaceBetween: 30
          }
        }
      });
    });
  }
});